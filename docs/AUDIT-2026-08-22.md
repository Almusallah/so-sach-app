# Audit completo — 2026-08-22
*15 agenti (4 finder → triage → verifica avversaria). 40 grezzi → 9 CONFERMATI con riproduzione, 1 refutato, 21 in coda non verificati.*

## Confermati (evidenza riprodotta)

### 1. [HIGH/hours] Bot goes dead on voice notes, files and stickers
**Area:** Zalo bot — event router (server.js:261-568)

**Evidenza:** handleZaloEvent has branches only for user_send_image/user_send_text and no default; live test confirmed audio/sticker/file events are received then answered with nothing — the exact silent-failure mode the codebase calls its worst. A VC sending a voice note during the demo sees a broken bot.

**Verifica:** Static: handleZaloEvent (/Users/apple/Developer/so-sach/server.js:261-570) is a single if/else-if on event.event_name with exactly two branches — 'user_send_image' (line 272) and 'user_send_text' (line 336) — and no default/else before the chain closes at line 568; any other event type falls straight through to the catch-less end and produces no reply. Only 'follow' is deliberately unanswered (comment at lines 264-271, welcome handled OA-side); audio/sticker/file are simply unhandled. Live repro: ran the server on port 3520 with dummy Zalo creds and a fetch stub logging every outbound Zalo API call, then POSTed four correctly-signed webhook events (user_send_text, user_send_audio, user_send_sticker, user_send_file). All four returned HTTP 200 {"ok":true} and logged 'zalo webhook ←' reception. The text event triggered an outbound reply (OUTBOUND-ZALO→ https://openapi.zalo.me/v3.0/oa/messa

**Fix:** Add a default branch that replies in the profile language — audio/file → 'mình chưa đọc được, bạn gõ số tiền hoặc chụp hoá đơn nhé', sticker/like acked silently.

### 2. [HIGH/hours] '📊 Quý này' menu button routes to the generic menu, not the quarter
**Area:** Zalo OA persistent menu / command router (src/commands.js normalize)

**Evidenza:** matchCommand('📊 Quý này') and fuzzy both return null (emoji is >1 edit), while 'quý' routes correctly; if the OA button sends its emoji label, the star moment of the demo answers with the command list. Demo-lethal.

**Verifica:** Reproduced by executing the repo's own modules. normalize() (src/commands.js:14-18) strips only diacritics/đ, never emoji: normalize('📊 Quý này') → '📊 quy nay'. Node run: matchCommand('📊 Quý này') → null (quarter match list at commands.js:67 lacks any emoji form), matchCommandFuzzy → null (emoji+space = 3 code units, >1 edit, fails the length gate in withinOneEdit at commands.js:110), while 'Quý này' and 'quý' both → 'quarter'. Full router replication: parseKhaiCommand, parseMoneyCommand, matchLangCommand, exact and fuzzy all null for '📊 Quý này', so the user_send_text handler falls to the final else at server.js:562-566 and sends menuText — the generic command list. Caveat: the Thanh menu button label lives in Zalo OA Manager, not the repo (server.js:268 comment), so 'the OA button sends its emoji label' is the finding's own stated conditional; the code-side failure it predicts is fully

**Fix:** Strip leading emoji/symbols in normalize() so the labelled button routes to quarter, and tap the real button once before the demo to confirm.

### 3. [HIGH/day] Typed money entries break on any extra word or slang, dumping the user into the menu wall
**Area:** Zalo bot — text parser (src/amount.js:21-75)

**Evidenza:** Fully-anchored regexes; live tests show 'thu 2tr4 hôm qua', 'thu 2tr4 cà phê', 'thu 2 chai' all write no entry and fall to the full menu — directly contradicting the marketed 'viết kiểu nào cũng hiểu'. Slang units chai/xị/lít unsupported.

**Verifica:** Reproduced against /Users/apple/Developer/so-sach at HEAD f5e629a. (1) Anchored regexes: src/amount.js:37 (^...$ for tr/triệu), :44 (^...$ for k/nghìn), :51-52 (plain path requires the WHOLE string to reduce to digits) — parseAmount rejects any trailing text; slang units chai/xị/lít appear nowhere in amount.js. (2) parseMoneyCommand (src/amount.js:61-75) returns null when the post-prefix remainder fails parseAmount, discarding the recognised THU/CHI prefix. Node repro: 'thu 2tr4' → {thu, 2400000} but 'thu 2tr4 hôm qua' → null, 'thu 2tr4 cà phê' → null, 'thu 2 chai' → null, 'chi 500k tiền điện' → null. (3) Routing: with money=null, matchCommand and matchCommandFuzzy also return null for all three phrases (verified via node), so the message hits the final else at server.js:562-566, which sends the full menuText — no entry is written and no targeted hint given despite the explicit thu/chi i

**Fix:** Accept a trailing free-text tail as description, recognise hôm qua/hôm kia and chai/xị/lít units, and on a THU/CHI prefix with unparseable amount reply a targeted hint instead of the full menu.

### 4. [HIGH/day] App section overflows the phone viewport — amounts clipped, delete button off-screen at 390px
**Area:** Web UI — mobile layout (styles.css .cards/.app-grid)

**Evidenza:** Screenshot-verified at 390×844 with the sample book loaded (scrollWidth 425 vs 390): 9-digit amounts and the 🗑 delete column render off the right edge because grid children default to min-width:auto and .cards keeps 3 columns ≤560px. This is exactly the demo 'try-it' configuration.

**Verifica:** Reproduced live: ran `PORT=3523 node server.js` in /Users/apple/Developer/so-sach, opened http://localhost:3523 at a 390x844 viewport, clicked the demo button 'Xem thử với sổ mẫu 4 tháng' (the try-it configuration). Measured document.documentElement.scrollWidth = 423 vs visualViewport.width = 390 (finding claimed ~425 — matches within font variance); before loading the sample scrollWidth was 390, so the sample data triggers it. 35 elements sit past x=390 outside any scroll container, including the second .panel (right edge 423). Screenshot-verified: the third summary card '149.680.000đ / Lãi gộp' (9-digit amount) is visibly cut off at the right screen edge, and the tax-threshold row '302 triệu dự kiến cả năm' touches the edge. Delete button: inside the ledger (.ledger has overflow-x:auto at /Users/apple/Developer/so-sach/public/styles.css:266, clientWidth 268 vs scrollWidth 331), the 🗑 .

**Fix:** Set minmax(0,1fr) on .cards and .app-grid, shrink card font ≤560px, and keep the ledger in a horizontal scroller with the delete control reachable.

### 5. [HIGH/day] Entire fundraising pack still describes last week's product
**Area:** Fundraising pack (docs/ decks + briefs)

**Evidenza:** Keyword scan of the EN/IT decks and briefs: zero hits for Sheets, claim link, bilingual, legal watch, or sửa/khai; slide 03, Brief §02 and the Roadmap 'Now' row all predate yesterday, and the '72 tests' proof point is stale (suite is 97 passing). Partner-facing and out of date.

**Verifica:** Reproduced against /Users/apple/Developer/so-sach (main, clean, f5e629a). (1) Keyword scan of extracted text from SoSach_Deck_PostNDA_EN/IT.pdf, SoSach_Brief_EN/IT.pdf, SoSach_Pitch_EN.pdf, SoSach_Teaser_PreNDA_EN.pdf in docs/: zero hits for 'sheets', 'claim link', 'bilingual/bilingue', 'legal watch', 'sửa', 'khai' — yet these features are live on main (commits 21f8698 'Sheets sync, the language layer…', a9c0ee2 '…the claim link…', f5e629a legal-watch alert; code at src/sheets.js, src/claim.js, server.js). (2) Stale proof point confirmed: deck page 5 reads 'tests written before the pilot — 72 of them' (source: docs/make_deck_postnda.py:267-268, EN and IT), while `npm test` reports '# tests 97 / # pass 97 / # fail 0'. (3) Roadmap 'Now' row (docs/make_deck_postnda.py:583) lists 'Zalo bot, ledger, score, declaration, agent dashboard, billing rails' — no Sheets sync, claim link, or language 

**Fix:** Regenerate both language decks and briefs to add Sheets sync, Zalo→web claim, the EN/VI language layer and typo-tolerant parser to slide 03/Roadmap-Now, bump 72→97 tests, and add Sheets + legal-watch rows to the moat and data-asset slides — keeping EN/IT in lockstep.

### 6. [HIGH/hours] Google Sheets sync is invisible on every prospect-facing site surface
**Area:** Website (public/index.html, live sosach.com.vn)

**Evidenza:** Hero, #how, #ways, #proof, Zalo banner and footer contain zero Sheets mention; the only Sheets UI is the post-login account modal. Yesterday's flagship user-owned-webhook trust feature is undiscoverable by any prospect (live site byte-identical to local).

**Verifica:** Reproduced. (1) grep -in 'sheet|trang tính' /Users/apple/Developer/so-sach/public/index.html matches only lines 23-24 (Google Fonts <link> and stylesheet tags) — zero Sheets content in hero, #how (L107), #ways (L128), #proof (L182), #zalo banner (L275) or footer; index.html renders no data-i18n key starting with sh_ (only dash_title at L239). (2) All Google Sheets copy (sh_title/sh_sub etc., public/app.js:205-223, comment 'Google Sheets (modal Tài khoản)') is rendered exclusively by sheetsSection inside accountModal() (public/account.js:148, 161-176, 215), which is opened only via the #meBtn account modal (account.js:95) and reads /api/ledger with the stored ss_token — i.e. post-login only. (3) Bot menuText (src/commands.js:208-236) linked-account hint mentions only the Zalo link code and đại lý thuế sharing, no Sheets in VI or EN. (4) Live-site parity verified: curl of both https://sosa

**Fix:** Add a #how card / trust chip 'Sổ tự chép sang Google Sheet CỦA BẠN — bạn giữ chìa khoá' (+EN, reusing the sh_sub copy) and mention it in the bot's menuText linked-account hint.

### 7. [HIGH/hours] Public README cites dead law and the wrong tax threshold
**Area:** README (public GitHub mirror so-sach-app)

**Evidenza:** Cites Thông tư 40/2021 twice, frames Decreto 70/2025 as living law, and states the threshold as 500M when NĐ 141/2026 raised it to 1 tỷ (the code itself uses 1B); feature list omits Sheets/claim/language/sửa-khai. This is where a diligencing partner looks.

**Verifica:** Reproduced in /Users/apple/Developer/so-sach/README.md. (1) Thông tư 40/2021 cited twice as current basis: line 12 ("mod. 01/CNKD (Thông tư 40/2021/TT-BTC)") and line 29 ("Soglie e aliquote hộ kinh doanh (Circ. 40/2021; riforma 2026)"); docs/LEGAL_BASELINE.md line 12 states TT 50/2026/TT-BTC replaces 40/2021 from 01/01/2026. (2) Line 5 presents Decreto 70/2025 as the living e-invoice law; LEGAL_BASELINE.md lines 15-16 say it is abrogated by NĐ 254/2026/NĐ-CP (in force 01/07/2026) and "70/2025 e 123/2020 = SOLO storia, mai legge viva". (3) Line 11 states the taxability threshold as "500M (soglia tassabilità 2026)", but src/tax.js:15-18 documents the raise from 500M to 1 tỷ per NĐ 141/2026/NĐ-CP and sets taxFree = 1_000_000_000 — the README contradicts the shipped code. (4) Product list (README lines 9-12) omits shipped features: src/sheets.js and src/claim.js exist on main and are nowhere

**Fix:** Rewrite the why-now paragraph and product list against LEGAL_BASELINE.md (TT 50/2026, 1 tỷ threshold, current features) and add the README to the legal-watch baseline so the sweep covers it.

### 8. [HIGH/hours] Every unrecognised message silently invalidates the claim link already sent
**Area:** Zalo→web claim funnel (server.js:199-202,566; claim.js:79)

**Evidenza:** claimReminderFor re-mints on every 'menu' and every unknown text, and each mint rotates the current hash so the previous token verifies 'invalid'; live rotation confirmed. The one-time CTA link attached to the user's first magic moment dies the instant they type anything unrecognised — the highest-value conversion tap and a live dead-end tap in front of an investor.

**Verifica:** Reproduced with the project's own module. Chain: server.js:560 first manual entry appends claimCtaFor -> claimLinkFor (server.js:182-183) which calls mintClaimToken; server.js:566 (unknown-text fallback) and server.js:527 ("menu") both append claimReminderFor (server.js:199-202) which calls claimLinkFor again, i.e. a fresh mint on every such message for any non-linked uid. src/claim.js:56 makes each mint overwrite the registry's current hash, and src/claim.js:79 rejects any token whose hash is not current with {error:"invalid"}. Executed: node -e importing src/claim.js — minted CTA token verifies as {"zaloId":"zalo-user-1"}; after one simulated unknown-message mint, the same CTA token returns {"error":"invalid"} while only the newest token verifies. The server's persistent registry (server.js:165-178, claimTokenHash on the zalo book) carries the rotation across restarts. Mitigation exist

**Fix:** Reuse the current unexpired token in claimLinkFor, minting only when none exists or it has expired.

### 9. [MEDIUM/day] Fuzzy matcher routes ordinary short words to the destructive delete command
**Area:** src/commands.js:108-166 + server.js:411-445

**Evidenza:** matchCommandFuzzy returns 'fix' for 'sea','six','sun','spa','sum' and a large ?oa/s?a/su? class; the fix branch splices the latest entry with no confirmation. A spa owner typing 'spa' or an EN-profile user typing 'six' silently loses their most recent fiscal entry — the denylist is unwinnable and doesn't cover English.

**Verifica:** Reproduced by executing the actual module: `node -e 'import("./src/commands.js")...'` in /Users/apple/Developer/so-sach returns matchCommandFuzzy('sea')='fix', 'six'='fix', 'sun'='fix', 'spa'='fix', 'sum'='fix' (also 'sia','sub','fox','sha' → 'fix'). Cause: src/commands.js:56 gives the fix command match aliases ["sua","xoa","sua lai","undo","delete","fix"], and matchCommandFuzzy (src/commands.js:153-166) accepts any input within edit distance 1 of any alias; 'spa'/'sun'/'sum'/'sea' are 1 substitution from 'sua', 'six' is 1 from 'fix'. The guards (length<3, digits, 6-char codes, MONEY_WORDS, VN_STOP_WORDS at lines 125-140) cover Vietnamese collision words only — no English words are in the denylist. Routing: server.js:385-386 `const cmd = matchCommand(rawText) || (khai || money || langSwitch ? null : matchCommandFuzzy(rawText))` feeds the fuzzy result straight into the dispatcher, and ser

**Fix:** Never fuzzy-route to a destructive command — restrict fuzzy matching to menu/quarter/year and require an explicit 'reply 1 to delete' confirmation before the fix branch mutates.

## Refutato dalla verifica avversaria

- Claim token is an unbound bearer credential enabling full account takeover

## Coda lunga (non verificata, da triage)

- [medium/hours] **Homepage hero demos a command that doesn't exist and stays Vietnamese in EN mode** — Change the mockup to real commands ('quý' with the quarter reply, 'sổ' as the year format) and add data-i18n / EN variants for the chat bubbles.
- [medium/hours] **Web forms and api() fail silently on bad input or dropped network** — Wrap api() in a catch that toasts 'Mất mạng — thử lại / No connection — retry' and show an inline error on the falsy-amount early return and on !r.ok in both save paths.
- [medium/hours] **EN demo path: declaration modal leaks Vietnamese and the 0đ tax is unexplained** — Add EN pairs for exemptNote and use vnDate for generatedAt, and either narrate the 0đ line deliberately or seed a >1 tỷ demo book that shows real GTGT/TNCN rows.
- [high/days] **Agent dashboard can't produce a filing or show sync/link/staleness status** — Add zaloLinked/sheetsConnected/lastEntryAt dots to the clients payload and a per-client 'Xem tờ khai' + CSV rendered from the declaration data already in ledgerPayload.
- [medium/hours] **Low-confidence photo proposals die silently; a late '1' triggers the absurd 'ghi 1đ?' prompt** — Special-case bare 1/2/3 with no pending → 'ảnh đó hết hạn rồi, gửi lại nhé' and prepend a one-line notice when a new photo displaces an unconfirmed proposal.
- [medium/days] **Fiscal writes are acknowledged before they are durable (fire-and-forget Postgres)** — Await persistence on the critical mutation paths before acknowledging (or adopt an outbox), surface failures with retry/alert instead of console.error, and order the merge's UPSERT before the zalo-boo
- [medium/hours] **Zalo webhook fails open — signature enforced only when the OA access token is configured** — Gate signature enforcement on the presence of the signing key (webhookSecretStatus().configured), rejecting bad/absent signatures always, and refuse to process mutating events when no key is configure
- [medium/hours] **Web entries carry no provenance — the 'Gốc' column is blank in CSV and Sheets** — Have the web client send provenance ('photo' from the extract-confirm modal, 'manual' from the manual form) and default it to 'manual' server-side.
- [medium/hours] **'Export CSV to see all' hint points at a button demo users can't see** — Render a 'Xuất CSV' link next to the more_entries hint (uid for anonymous, token fetch for logged-in), or reword to 'đăng nhập để xuất CSV' when logged out.
- [medium/day] **Onboarding collects monthly revenue then drops it — missing the mid-year over-threshold warning** — If revenueEstimate×12 crosses the threshold and the book is empty mid-year, auto-open the opening-balances modal or show a persistent dashboard banner warning the quarters are untracked.
- [medium/hours] **One shared 30-req/15-min IP bucket covers the whole claim funnel — CGNAT lockout with raw JSON** — Raise the bucket and key the page GET separately from preview/POST, and serve the styled claim page with a friendly retry state on 429 instead of raw JSON.
- [medium/hours] **Compliance-currency story (weekly legal watch) is built but untold anywhere** — Add one line under the site decree chip and one bullet on the deck correctness slide — 'the same discipline now watches the law itself, weekly.'
- [medium/hours] **The claim link's retention promise is told only to users who already have the product** — Add the phone-loss / agent-visibility promise as a line in the #zalo banner and the hero trust row.
- [medium/hours] **Sheets connect errors vanish in a 2.2-second toast** — Add a persistent inline status/error line under the Connect button using the existing sh_last_fail state that already survives re-render.
- [medium/hours] **pushToSheet reads the Apps Script response with no size cap** — Reject responses over a small Content-Length and read at most a few KB before aborting — the Apps Script contract only needs a tiny {ok,at|error} JSON.
- [medium/hours] **Claim-link supersession is not durable across restarts** — Persist the current claim hash keyed by zaloId even before a book exists, so supersession survives redeploys.
- [medium/hours] **Zalo OA welcome undersells the bot and the persistent menu has no source of truth** — Extend welcomeText with 'Gõ "menu" để xem hết' + an EN-availability note, mirror it to OA Manager, and add a documented MENU source block in commands.js so the menu can't silently diverge.
- [low/hours] **Web ledger shows raw ISO dates the code itself calls 'noise for a market lady'** — Apply the vnDate dd/mm/yyyy formatter in the web ledger, dropping the year for current-year rows to stop the wrap.
- [high/hours] **[KNOWN/PARKED] Pending deploy of f5e629a — live declaration still cites dead law** — Deploy main (f5e629a) to Render before the coffee — a one-line citation sweep, nothing else on the path changes.
- [high/hours] **[KNOWN/PARKED] Render free-tier cold start can blank the site mid-demo** — Open the site on the demo phone ~2 min before the meeting (and after any lull), or add an uptime pinger / temporarily upgrade the web service for demo week.
- [low/hours] **[KNOWN/PARKED] Confirm the meeting frame before choosing the leave-behind** — If the coffee is a strategic partner, hand this brief as-is; if it's an investing fund, bring SoSach_Teaser_PreNDA_IT / the fundraising pack instead.
---

## Aggiornamento 2026-08-22 sera — treno "durabilità + dashboard đại lý"

**RISOLTI in questo treno** (commit successivo a `f87672e`):
- ✅ **Fiscal writes acknowledged before durable** → `src/store.js`: coda per chiave + retry (250/500ms, timeout pool 3-4s) + `mutateBookDurable` (snapshot→mutazione→persist→rollback). OGNI sito di mutazione fiscale ora aspetta il disco prima di confermare (bot: risposta "chưa lưu được, thử lại"; web: 503 con lo stesso messaggio). Merge Zalo→account riscritto sotto ENTRAMBE le code (`mergeBooksDurable`): destinazione durevole PRIMA della rimozione della sorgente; scritture concorrenti durante il merge re-instradate (`writeZaloEntry`). Claim/link riordinati merge-prima-link: nessuno stato "collegato a metà" durevole. payOS webhook: rollback anche di `sub` (retry non regala più +30 giorni). 13 unit store (incl. interleave reale via delay-hook) + 10 e2e durabilità (8d).
- ✅ **Agent dashboard can't produce a filing / show status** → roster con `zaloLinked`, diagnostica Sheets (senza url/secret), `lastEntryAt`, voto; `GET /api/agent/client/:phone/declaration?year&q` (01/CNKD per cliente, preparatore = đại lý, selettore ultimi 4 trimestri nella UI) + `/export.csv` per cliente (ownership-checked). 10 e2e (8e).
- ✅ (dal verifier pre-commit) **CSV formula injection** neutralizzata (=+-@ → apostrofo) — il CSV per-cliente attraversa un confine di fiducia; **/api/agent/client/:phone** non espone più l'URL del foglio del cliente; escHtml sui nomi cliente nelle superfici đại lý.

**Nota di coda lunga aggiornata**: "rollback account lato-chiamante non serializzato" (classe NIT, bassa probabilità — i libri hanno la coda, gli account no) registrato come debito consapevole.
